# <模块名> 测试指南

> 状态：文档场景，待测试部执行

## 1. 功能与测试范围

**本次覆盖：** [填写提示：按用户清单或代码证据归纳，用逗号分隔]

**不在范围：** [填写提示：列出算法精度、整机 OTA 等与本模块无关的项目]

## 2. 测试基线

| 项 | 说明 |
| --- | --- |
| 被测模块 | `<进程名或可执行文件名>` |
| 产品/平台 | [填写提示：如 lrd-w/rk3588，按实机填写] |
| 软件版本 | [填写提示：如跟随 app 版本，使用 `syskit info`；不要把 /app/bin 路径写入本表] |
| 配置基线 | [填写提示：填写仓库内配置模板的相对路径] |

部署路径见 §4.2（若有）。

## 3. 话题与接口

[填写提示：无 LYOS Topic 时，将本节改为“对外接口”，列出 API/CLI/文件/HTTP]

话题/接口名以实机配置为准。

| 方向 | 名称（默认/示例） | 类型 | 频率或触发 | 备注 |
| --- | --- | --- | --- | --- |
| 发布 | | | | |
| 订阅 | | | | |

[填写提示：多实例或多产品时增加差异表，并列出 status/control 等共用接口的关键字段]

## 4. 环境准备

### 4.1 硬件与网络

### 4.2 软件

- 二进制与配置路径（目标机）
- 所需诊断工具（如 `lyostopic`）

### 4.3 配置检查（启动前）

## 5. 启动、停止与状态检查

### 5.1 启动

```bash
# 启停命令（含 system_manager 自启说明若适用）
```

**成功判据：** [填写提示：使用进程状态和日志关键词作为精简判据]

### 5.2 运行中

### 5.3 停止

```bash
# robot_control / Ctrl+C / kill
```

## 6. 测试命令速查

```bash
# --- 接口验证（按模块替换话题名）---
# lyostopic list | grep ...
# lyostopic echo <topic>
# lyostopic hz <topic>          # 预期频率
# lyostopic pub <topic> <msg_type> '<json>'

# --- 进程 / 网络 / 配置 ---
# ps aux | grep <进程名>
# syskit info
```

**日志关键词：**

| 含义 | 关键词 |
| --- | --- |
| | |

## 7. 测试场景

> 待执行。

| 编号 | 场景 | 前置 | 步骤 | 预期 | 观察（命令/现象） |
| --- | --- | --- | --- | --- | --- |
| T01 | 正常路径 | | | | `lyostopic hz …` ≈ … |
| T02 | 边界 | | | | |
| T03 | 异常 | | | | |

## 8. 常见问题与恢复

| 现象 | 检查 | 恢复 |
| --- | --- | --- |
| | | |

## 9. 测试反馈信息

1. 产品/平台；2. 版本（`syskit info` 等）；3. 实际配置；4. 日志；5. 网络/中间文件快照；6. 复现步骤与期望/实际差异。
