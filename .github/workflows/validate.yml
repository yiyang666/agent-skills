name: 校验 Skills

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: 检出仓库
        uses: actions/checkout@v4
      - name: 校验 Skill 结构与脚本
        run: python3 scripts/validate_repo.py
